# 浏览器是什么
浏览器==HTTP请求的**生成器+解析器+渲染器**
# 网络是一个"分层运输系统"
- 应用层: DNS(应用层协议): 把googel.com变为IP
生成HTTP/HTTPS请求
- 传输层(TCP/UDP协议): TCP是可靠传输,UDP是快速传输
- 网络层(IP): 确定目标IP和路由
- 链路层(网卡): 发送电信号/WIFI信号
路由器/互联网: 多跳转发
google服务器: 返回HTML
- 浏览器: 渲染页面
# 访问网站时:
访问网站时，URL 中的 http 或 https 决定使用 HTTP 还是 HTTPS 协议；浏览器会自动进行 DNS 解析获取 IP 地址；然后应用层生成 HTTP 请求，再由 TCP/UDP 进行传输封装，IP 层负责寻址，最终通过网卡发送出去。整个过程是分层封装，而不是顺序执行。
# Clash介绍
**Clash=本地流量分流+代理调度器**
作用:
- 接管流量(通过系统代理/虚拟网卡)
- 按规则判断走向
- 决定: 直连/走代理/丢弃
# 大航海订单(subscription URL,订阅链接)
它提供节点列表(美国,日本,新加坡...)
每个节点其实是一个 SOCKS/HTTP/Vmess/Reality 服务器
# clash的端口
只有**被系统代理/TUN接管的流量**,才会被送到clash的端口
# clash的代理模式
系统代理 && TUN模式 && 
## clash的系统代理
Windwos开启系统代理后,支持系统代理设置的软件会将HTPP/THHPS请求发送到本地clash代理端口,clash接收到这些请求后,根据(全局/规则/直连)模式决定走or不走代理
## TUN(Tunnel Interface,隧道接口)模式
本质上是一个虚拟网络设备(虚拟网卡)
TUN 模式通过创建并接管一个虚拟网卡，让系统所有网络流量在 IP 层被 Clash 捕获，Clash 再根据规则决定这些流量是走代理还是直连。
## 系统代理&&TUN模式的对比
|代理|作用层||
|-|-|-|
|系统代理|应用层||
|TNU模式|网络层||


