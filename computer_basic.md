# DNS域名解析
域名解析为公网IP地址
eg: google.com-->IP地址
## 域名解析
实际生活中一个网站的域名!=一个固定服务器
而是: 多台服务器 全球节点 云服务器集群 负载均衡系统...
eg: google.com --> 今天: IP A
    google.com --> 明天: IP B
    google.com --> 不同地区: IP C
## DNS解析流程
- 假设解析baidu.com的IP地址
- 查浏览器缓存
- 查路由器缓存
- 如果没有: 发送请求给DNS服务器(UDP/TCP)
- DNS服务器递归查询: 根DNS-->顶级DNS-->权威DNS
- 返回IP
- 写入缓存

(浏览器的缓存会过期
如果解析baidu.com时,缓存没有过期但没联网,仍然可以解析到IP地址,但是访问baidu.com是不可以的,因为没有联网,不可以发送网页请求)
## DNS解析域名时,域名会过期的原因
IP为什么会变?
- 负载均衡: 一个网站访问人太多,会分配到不同的服务器
- CDN(内容分发网络): 
                  日本用户-->日本节点IP
                  中国用户-->中国节点IP
                  美国用户-->美国节点IP
- 故障切换: 某一台服务器挂了,IP会指向备用服务器
# 浏览器是什么
浏览器==HTTP请求的**生成器+解析器+渲染器**
# 网络是一个"分层运输系统"
- 应用层: DNS(应用层协议): 把googel.com变为IP
生成HTTP/HTTPS请求
- 传输层(TCP/UDP协议): TCP是可靠传输,UDP是快速传输
- 网络层(IP): 确定目标IP和路由
- 链路层(网卡): 发送电信号/WIFI信号
路由器/互联网: 多条转发
google服务器: 返回HTML
- 浏览器: 渲染页面
# 访问网站时:
网络是**分层封装+同时协作**
|步骤|完成的工作|备注|
|-|-|-|
|应用层|HTTP/HTTPS请求 DNS解析域名||
|传输层|TCP/UDP||
|网络层|IP-->决定下一次跳转||
|链路层|数据(电信号/WIFI信号)从网卡发出去||
|互联网|路由器逐条转发||
|服务器|Google返回数据||
|浏览器|渲染页面||
# clash
**Clash=本地流量分流+代理调度器**
作用:
- 接管流量(通过系统代理/虚拟网卡)
- 按规则判断走向
- 决定: 直连/走代理/丢弃
## 大航海订单(subscription URL,订阅链接)
它提供节点列表(美国,日本,新加坡...)
每个节点其实是一个 SOCKS/HTTP/Vmess/Reality 服务器
## clash的端口
只有**被系统代理/TUN接管的流量**,才会被送到clash的端口
## clash的代理模式
系统代理 && TUN模式 && 
### clash的系统代理
只针对: 支持系统代理的软件流量
流程:
应用(浏览器)
↓
系统代理(127.0.0.1:7897)
↓
clash
↓
规则判断决定: 直连/代理/拒绝
↓
发送给目标服务器
### TUN(Tunnel Interface,隧道接口)模式
本质上是一个虚拟网络设备(虚拟网卡)
TUN 模式通过创建并接管一个虚拟网卡，让系统所有网络流量在 IP 层被 Clash 捕获，Clash 再根据规则决定这些流量是走代理还是直连。
### 系统代理&&TUN模式的对比
|项目|系统代理模式|TUN(虚拟网卡模式)|
|-|-|-|
|覆盖范围|部分应用|几乎所有流量|
|工作层级|应用层|网络层|
|是否依赖软件支持|需要|不需要|
|是否代理游戏|通常不行|可以|
|原理|代理设置|网卡劫持路由|
|是否需要服务|不需要|需要|
|人话|应用自己选择走代理|系统强制把流量交给代理|
# Windows的VMware上运行Linux操作系统
在Windows虚拟机里的Linux操作系统,网络请求一定会经过宿主机,但"怎么经过"取决于虚拟机的网络模式
Linux(虚拟机)
↓
虚拟网卡
↓
真实网卡(WIFI/网线)
↓
路由器
↓
互联网
## 虚拟机的三种网络模式
NAT模式 
### NAT模式
- 工作方式
Linux虚拟机-->虚拟NAT网卡-->Windows(做路由+转发)-->公网
- 特点
Linux可以上网,但外界看不到Linux,所有流量都经过Windows转发
- 类比
Linux==房间里的电脑,Windows==帮你打电话的前台
### Bridge桥接模式
- 工作方式
Linux虚拟机-->直接连接局域网路由器-->互联网
- 特点
Linux像独立的设备
有自己的IP
可以被局域网访问
实际仍然通过宿主机的物理网卡
### Host-only(仅主机模式)
只能宿主机和虚拟机通信
Linux <--> Windows
## 给虚拟机Linux操作系统配置Windows中的clash代理
- 1.打开clash allow LAN,允许局域网连接
- 2.查看Windows内网IP
- 3.Linux设置proxy=Windows内网IP:7897 (7897端口是Windows的clash监听端口)
(本质:只要Linux可以顺畅访问Windows的7897端口,就可以使用Windows代理)
# git remote set-url origin git@github远程仓库地址
是设置远程仓库的地址为 github 上的地址，使用 ssh 连接


